# Report: Agent Skill Installation Plan (For Codespace)

**Agent:** Buffy
**Date:** 2026-07-15
**Task Type:** Installation Plan
**Domain:** general

---

## 1. Executive Summary

Due to slow network on local Windows machine, the following skills could not be installed via `npx skills add`. This document lists all remaining skills to install on GitHub Codespace (or any machine with a faster connection), plus the one successfully installed skill (`vitest`) for reference.

---

## 2. Successfully Installed (already in `.agents/skills/`)

The following skill was installed successfully:

| Skill | Source | Command Used |
|-------|--------|-------------|
| `vitest` | `antfu/skills` | `npx skills add antfu/skills --skill vitest --yes` |

---

## 3. Remaining Skills to Install — Core 7

Install these on Codespace. Run each command one at a time:

```bash
# 1. Database Migrations (for multi-tenancy schema-per-entity)
npx skills add sickn33/antigravity-awesome-skills --skill database-migrations --yes

# 2. Auth Implementation Patterns (for platform operator auth)
npx skills add sickn33/antigravity-awesome-skills --skill auth-implementation-patterns --yes

# 3. Next.js + Supabase Auth (for MFA step-up gate)
npx skills add sickn33/antigravity-awesome-skills --skill nextjs-supabase-auth --yes

# 4. Security Audit (for operator authorization & safe-action allowlist)
npx skills add sickn33/antigravity-awesome-skills --skill security-audit --yes

# 5. Audit Logging (for public.activity_events patterns)
npx skills add bagelhole/devops-security-agent-skills --skill audit-logging --yes

# 6. Test-Driven Development (for service layer & RPC testing)
npx skills add sickn33/antigravity-awesome-skills --skill test-driven-development --yes
```

### ⚠️ Note on `database-migrations`

The name `database-migrations` may be ambiguous. If the above command fails, try these alternatives:

```bash
npx skills add sickn33/antigravity-awesome-skills --skill database-migrations-sql-migrations --yes
# or
npx skills add sickn33/antigravity-awesome-skills --skill database-migration --yes
```

---

## 4. Bonus Skills to Check

### 4.1 Agent Squad (Multi-Agent Code Pipeline)

These 8 sub-skills form a complete agent pipeline for parallel code development. Install all of them:

```bash
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/alex --yes       # Requirements → plan
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/aria --yes       # Data model & API contracts
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/mason --yes      # Clean functional code
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/max --yes        # Code cleanup
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/quinn --yes      # Test suites
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/luna --yes       # Code review
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/dep --yes        # Deployment/CI-CD
npx skills add sickn33/antigravity-awesome-skills --skill agent-squad/rex --yes        # Spec writing
```

### 4.2 Pony Tail Skill

```bash
npx skills add dietrichgebert/ponytail --skill ponytail --yes
```

### 4.3 Using Superpowers Skill

```bash
npx skills add obra/superpowers --skill using-superpowers --yes
```

### 4.4 Andrej Karpathy Perspective

```bash
npx skills add alchaincyf/karpathy-skill --skill andrej-karpathy-perspective --yes
# Alternative:
npx skills add szkocot/andrej-karpathy-skills --skill karpathy-guidelines --yes
```

---

## 5. Verification After Installation

After all skills are installed, verify with:

```bash
ls .agents/skills/ | grep -E "(database|auth|nextjs|vitest|security|audit|test-driven|ponytail|superpower|karpathy|agent-squad)"
```

Should show all installed skill directories.

---

## 6. Index Update Reminder

After installation on Codespace, update `docs/Projectskillindex.md` to add each newly installed skill with its name and description. The vitest entry has already been added.

---

*Report generated by Buffy*
